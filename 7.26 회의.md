## 1. 선택 이유

전에 사용한 EDA결과를 기반으로 선택함.

분석 결과 풍속이 발전량과 가장 강한 관계를 보임. 풍속이 증가할수록 발전량이 증가하다가 일정 구간 이후 포화되는 전형적인 풍력 파워커브 형태가 확인되었다. 또한 풍향, 시간 정보, 기온 등의 영향도 존재했지만 대부분 기상 변수 중심의 정형 데이터 문제에 가까웠다.

이러한 특성을 고려했을 때 LightGBM을 우선 선택한 이유는 다음과 같다.

* 풍속과 발전량 사이의 비선형 관계를 별도의 수식 없이 학습할 수 있음
* 변수 간 상호작용(풍속, 풍향, 계절성 등)을 자동으로 반영할 수 있음
* 결측치 처리에 강함
* 데이터 규모(그룹당 약 2~3만 행)에 비해 학습 속도가 빠름
* 실험 반복 비용이 낮아 다양한 피처와 검증 전략을 빠르게 비교할 수 있음

SCADA 데이터를 이용해 물리 기반 파워커브를 별도로 생성해 모델에 추가했지만 성능 향상은 확인되지 않았다. 이는 LightGBM이 이미 원시 풍속 정보만으로 파워커브 형태의 비선형 관계를 상당 부분 학습하고 있음을 의미한다.
또한 현재까지 실제 제출 결과를 기준으로 보면, LightGBM 기반 모델이 가장 안정적인 성능을 기록하고 있다. Grid Aggregation, Calibration, SCADA 파워커브 등 여러 변형을 시도했지만 최종적으로는 LightGBM + L1 Objective 조합이 최고 점수(0.6059)를 기록했다.

따라서 현재는 LightGBM을 베이스라인으로 유지하고 모델 자체를 교체하기보다는 평가 지표인 FICR를 더 잘 반영하는 학습 전략이나 새로운 정보원을 활용하는 방향에 우선순위를 두고 있다.

## 2. 수행한 주요 실험

### Grid Aggregation 최적화

기상 격자를 풍력단지 위치로 변환하는 방식(mean, nearest, IDW)을 비교했다.상관관계 분석과 Holdout 검증에서는 좋아 보였지만 리더보드 점수는 오르지 않았다.
현재는 안정적인 mean 방식을 사용하고 있다.

---

### Calibration

Isotonic Calibration을 적용해 예측값을 보정하는 실험을 수행했다.
다중 폴드 CV에서는 개선 신호가 있었지만 실제 제출에서는 성능이 하락했다.
분석 결과 그룹별 편차가 크고 과적합 가능성이 있다고 판단해 현재 파이프라인에서는 제외했다.

---

### SCADA 기반 파워커브

SCADA 데이터를 이용해 물리 기반 파워커브를 생성하고 보조 피처로 활용하는 실험을 수행했다.

Holdout 결과는 다음과 같다.

| 실험            | Holdout Score |
| ------------- | ------------: |
| Baseline      |        0.6241 |
| SCADA 보정 파워커브 |        0.6211 |
| 직접 파워커브       |        0.6237 |

상관관계는 증가했지만 예측 성능 향상은 확인되지 않았다.
분석 결과 LightGBM이 이미 원시 풍속만으로 파워커브 형태의 비선형 관계를 학습하고 있는 것으로 판단된다.


---

## 3. 현재까지의 판단

지금까지 수행한 실험들을 종합하면, 단순한 피처 추가나 데이터 재가공만으로는 성능 향상이 확인되지 않았다.
Grid Aggregation, Calibration, SCADA 파워커브 모두 일부 검증 지표에서는 개선 신호가 있었지만 실제 제출 성능 향상으로 이어지지 않았다.
또한 CV와 Holdout 결과가 실제 리더보드 결과와 일치하지 않는 사례도 반복적으로 확인되었다.
따라서 현재는 기존 정보의 재표현보다 새로운 학습 신호를 제공하거나, 평가 지표를 더 직접 반영하는 방향에 우선순위를 두고 있다.

---

## 4. 검토 중인 방향

금까지 수행한 실험들을 통해 단순한 피처 추가나 데이터 재가공만으로는 성능 향상이 확인되지 않았다. 따라서 앞으로는 기존 정보를 다른 형태로 가공하는 것보다, 현재 모델이 충분히 활용하지 못하는 신호를 찾거나 평가 지표를 더 직접 반영하는 방향을 우선 검토하고 있다.

###1순위: FICR을 고려한 학습 전략

현재 모델은 MAE(L1 Loss)를 최적화하고 있지만 실제 대회 평가는 NMAE와 FICR을 함께 사용한다. 특히 FICR은 오차 구간에 따라 보상이 달라지는 구조를 갖고 있어 단순 MAE 최적화만으로는 충분히 대응하기 어려울 수 있다. 따라서 향후에는 FICR의 보상 구조를 보다 직접 반영할 수 있는 학습 방법이나 손실함수를 검토할 계획이다.

2순위: 단순한 형태의 Calibration
기존 Isotonic Calibration은 과적합 가능성이 확인되었다. 이에 따라 자유도가 낮은 Binned Calibration, Piecewise Linear Calibration 등을 검토하고 있다.
다만 검증 단계에서 충분한 개선이 확인되는 경우에만 진행할 예정이다.

3순위: 그룹 간 정보 활용
현재는 그룹별로 완전히 독립적인 모델을 사용하고 있다.
(Group1과 2는 동일한 VESTAS 터빈이고 Group3은 다른 제조사(UNISON) 터빈을 사용,
발전 특성이 다를 가능성을 고려해 그룹별 독립 모델을 사용함.)

Group3은 다른 그룹보다 학습 데이터가 1년 적기 때문에 향후에는 Group1과 2에서 학습된 정보를 3에 적용할 수 있는지 볼 생각임. 다만 터빈 종류와 발전단지 특성이 다르기 때문에 단순히 정보를 공유하는 것이 항상 도움이 되는 것은 아니다. 따라서 현재 모델을 기준선으로 두고 그룹 간 정보 공유를 적용했을 때 Holdout 성능과 그룹별 성능이 실제로 개선되는지 확인한 뒤 적용 여부를 판단할 예정이다.
4순위: 다른 모델 계열 검토

현재까지는 LightGBM이 가장 안정적인 성능을 보이고 있다.
다만 회의에서 공유되는 결과를 참고하여 CatBoost, XGBoost 등 다른 모델 계열이 실제로 다른 오차 패턴을 보이는지 확인하고, 필요하다면 추가 실험을 진행할 계획이다.
---

## 5. 피처 엔지니어링 

EDA 결과를 바탕으로 풍속, 풍향, 시간 정보를 중심으로 피처 엔지니어링을 수행했다. 풍속 벡터를 속도와 방향으로 변환했고, LDAPS와 GFS 예보 차이를 나타내는 파생 피처를 추가했다. 또한 시간 정보를 반영하기 위해 월, 요일, 시간 및 주기성(sin/cos) 피처를 생성했다. 이 외에도 Grid Aggregation 방식 비교와 SCADA 기반 파워커브 피처 생성 실험을 수행했으나, 현재까지는 LightGBM이 원시 풍속 정보만으로도 대부분의 관계를 학습하는 것으로 보여 추가 성능 향상은 확인되지 않았다.
---

## 6. 정리

지금까지의 실험을 통해 단순히 기존 데이터를 다른 형태로 가공하는 것만으로는 성능 향상에 한계가 있다는 점을 확인했다. 앞으로는 모델이 현재 활용하지 못하고 있는 새로운 정보원을 추가할 수 있는지 검토하려고 한다. 예를 들어 현재는 LDAPS와 GFS 예보 데이터를 중심으로 학습하고 있는데 풍력발전량에 영향을 줄 수 있는 다른 기상 정보나 공간 정보, 또는 현재 파이프라인에서 활용하지 못하고 있는 데이터 신호가 있는지 확인할 계획이다. 지금까지 시도한 SCADA 파워커브의 경우 기존 풍속 정보를 재가공한 수준에 가까웠기 때문에 큰 효과를 보지 못했지만 모델이 지금까지 보지 못한 정보를 추가할 수 있다면 성능 향상 가능성이 더 높다고 생각한다.

또한 현재 사용 중인 LightGBM은 기본적으로 평균 오차를 줄이는 방향으로 학습하고 있다. 하지만 실제 대회 평가는 NMAE뿐만 아니라 FICR도 함께 반영되며 FICR은 특정 오차 구간(6%, 8%)을 기준으로 점수가 달라지는 구조를 가지고 있다. 따라서 단순히 평균 오차를 줄이는 것과 실제 대회 점수를 높이는 것이 항상 같은 방향은 아닐 수 있다. 앞으로는 FICR이 반영되는 방식까지 고려해 모델을 학습시키거나, 예측 결과를 보정하는 방법을 검토할 예정이다. 즉, 단순히 예측값을 더 정확하게 만드는 것뿐 아니라 대회가 실제로 평가하는 기준에 더 가까운 방향으로 모델을 개선해 보고자 한다.

추가로 현재 검증 방식에서 확인된 한계도 고려하고 있다. CV나 Holdout에서 보이는 작은 개선은 실제 제출 결과와 다르게 나타난 사례가 있었기 때문에, 앞으로는 수치가 조금 좋아졌다는 이유만으로 적용하기보다 왜 성능이 좋아졌는지 설명할 수 있는지, 실제로 새로운 정보를 활용하고 있는지, 평가 지표와 연결되는 근거가 있는지를 함께 확인하면서 실험을 진행할 계획이다.
