# BARAM 2026 풍력발전량 예측 프로젝트 진행 현황 및 향후 방향

**작성일:** 2026-07-26
**대회:** BARAM 2026 (DACON #236727)
**대상:** `daeun_claude_pipeline`

---

## 1. 현재 접근 방식

이번 대회는 과거 발전량을 이용하는 시계열 예측이 아니라, 하루 전 기상예보(LDAPS, GFS)만으로 발전량을 예측하는 회귀 문제에 가깝다.

EDA 결과에서는 풍속이 발전량과 가장 높은 상관관계를 보였고, 풍속과 발전량 사이에 강한 비선형 관계도 확인되었다. 또한 입력 데이터 대부분이 정형(Tabular) 데이터라는 점을 고려해 현재는 LightGBM을 기본 모델로 사용하고 있다.

현재까지 실제 제출 기준 최고 성능은 **LightGBM + L1 Objective** 조합에서 기록되었다.

| 구성                      | Total Score |
| ----------------------- | ----------: |
| LightGBM + L1 Objective |  **0.6059** |

현재 파이프라인은 이 구성을 기준선(Baseline)으로 유지하고 있다.

---

## 2. 수행한 주요 실험

### Grid Aggregation 최적화

기상 격자를 풍력단지 위치로 변환하는 방식(mean, nearest, IDW)을 비교했다.

EDA와 검증 단계에서는 일부 개선 신호가 확인되었지만, 실제 제출에서는 성능 향상이 확인되지 않았다.

현재는 가장 단순하고 안정적인 mean 방식을 사용하고 있다.

---

### Calibration

Isotonic Calibration을 적용해 예측값을 보정하는 실험을 수행했다.

다중 폴드 CV에서는 개선 신호가 있었지만 실제 제출에서는 성능이 하락했다.

분석 결과 그룹별 편차가 크고 과적합 가능성이 있다고 판단해 현재 파이프라인에서는 제외했다.

---

### SCADA 기반 파워커브

SCADA 데이터를 이용해 물리 기반 파워커브를 생성하고 보조 피처로 활용하는 실험을 수행했다.

Holdout 결과는 다음과 같다.

| 실험            | Holdout Score |
| ------------- | ------------: |
| Baseline      |        0.6241 |
| SCADA 보정 파워커브 |        0.6211 |
| 직접 파워커브       |        0.6237 |

상관관계는 증가했지만 예측 성능 향상은 확인되지 않았다.

분석 결과 LightGBM이 이미 원시 풍속만으로 파워커브 형태의 비선형 관계를 학습하고 있는 것으로 판단된다.

현재 파이프라인에는 반영하지 않았다.

---

## 3. 현재까지의 판단

지금까지 수행한 실험들을 종합하면, 단순한 피처 추가나 데이터 재가공만으로는 성능 향상이 확인되지 않았다.

Grid Aggregation, Calibration, SCADA 파워커브 모두 일부 검증 지표에서는 개선 신호가 있었지만 실제 제출 성능 향상으로 이어지지 않았다.

또한 CV와 Holdout 결과가 실제 리더보드 결과와 일치하지 않는 사례도 반복적으로 확인되었다.

따라서 현재는 기존 정보의 재표현보다 새로운 학습 신호를 제공하거나, 평가 지표를 더 직접 반영하는 방향에 우선순위를 두고 있다.

---

## 4. 검토 중인 방향

### 1순위: FICR을 고려한 학습 전략

현재 모델은 MAE 기반 손실함수를 최적화하고 있지만, 실제 대회 평가는 NMAE와 FICR의 조합으로 이루어진다.

따라서 FICR의 보상 구조를 보다 직접적으로 반영하는 학습 전략을 우선 검토하고 있다.

특히 커스텀 Objective 또는 FICR 근사 손실함수는 아직 시도하지 않은 영역이다.

---

### 2순위: 단순한 형태의 Calibration

기존 Isotonic Calibration은 자유도가 높아 과적합 가능성이 있었다.

Binned Calibration, Piecewise Linear Calibration과 같이 더 단순한 형태의 보정을 검토하고 있다.

다만 Holdout 기준에서 의미 있는 개선이 확인될 경우에만 진행할 계획이다.

---

### 3순위: 그룹 간 정보 공유

현재는 그룹별로 독립적인 모델을 사용하고 있다.

Group 3은 다른 그룹에 비해 학습 데이터가 적기 때문에 Group 1, 2의 정보를 일부 활용하는 구조도 검토하고 있다.

다만 제조사와 사이트 특성이 다르기 때문에 별도 검증이 필요하다.

---

### 4순위: 다른 모델 계열

CatBoost, XGBoost 등 다른 모델 계열도 검토 대상이다.

다만 기존 프로젝트 기록에서는 유의미한 개선 사례가 확인되지 않아 현재 우선순위는 높지 않다.

---

## 5. 회의에서 논의하고 싶은 내용

현재 방향은 지금까지의 실험 결과를 바탕으로 정리한 우선순위일 뿐, 확정된 결론은 아니다.

회의에서는 다음 내용을 중심으로 의견을 듣고 싶다.

* LightGBM + L1 기준선을 명확하게 상회하는 결과가 있는지
* 새로운 피처 또는 데이터 신호를 활용한 사례가 있는지
* FICR 개선 근거가 있는 접근이 있는지
* 현재 검증 방식(CV, Holdout)의 한계를 보완할 방법이 있는지

충분한 근거가 확인되면 현재 방향과 무관하게 적극적으로 반영할 예정이다.

---

## 6. 정리

현재까지는 **LightGBM + L1 Objective**가 가장 안정적인 기준선으로 확인되었다.

반면 Grid Aggregation, Calibration, SCADA 파워커브 등 기존 정보의 재가공은 실제 성능 향상으로 이어지지 않았다.

따라서 앞으로는 다음 순서로 검토를 진행할 계획이다.

1. FICR을 고려한 학습 전략
2. 단순한 형태의 Calibration
3. 그룹 간 정보 공유
4. 다른 모델 계열

회의 결과와 추가 실험 결과를 반영해 우선순위를 지속적으로 갱신할 예정이다.
