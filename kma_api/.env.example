# Copy to .env and fill in. .env is gitignored; this template is not.
#   cp .env.example .env && ${EDITOR:-nano} .env
#   set -a && . ./.env && set +a      # load into the shell before running scripts

# KMA API Hub key, issued at https://apihub.kma.go.kr/
KMA_API_KEY=

# 2.1 단기예보 endpoint, BASE PATH ONLY -- no query string, and never the key.
KMA_API_URL=https://apihub.kma.go.kr/api/typ01/cgi-bin/url/nph-dfs_shrt_grd
