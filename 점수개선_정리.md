# 점수 개선 과정 정리 — 팀원 버전(0.64129) → v35(0.64199)

## 요약

| 버전 | 총점 | 1−NMAE | FICR | 한 줄 |
|---|:---:|:---:|:---:|---|
| **팀원 (submit_A)** | **0.64129** | 0.87408 | 0.4085 | 최근창 구간별 편향 보정으로 2025 변화 반영 |
| **v35** | **0.64199** | 0.87288 | 0.41109 | 팀원 방식 위에 g1 보정을 최대 강도로 보강 |

기존 A모델(0.63625)에서 정체돼 있었는데, **팀원 버전이 0.64129로 뚫었고, 그 방식을 이어받아 v35에서 0.64199로 한 계단 더** 올렸다.

---

## 1. 팀원 버전이 점수를 끌어올린 방법 (0.63625 → 0.64129)

### 핵심 아이디어: 문턱(8%) 바로 위 구간을 겨냥

FICR은 오차가 **8% 이하일 때만** 정산금이 생기는 계단형 지표다.
- 오차 20%대는 아무리 줄여도 여전히 문턱 밖 → FICR 기여 0
- **오차 8~12%는 살짝만 밀면 8% 문턱을 넘어** 정산금 획득

그래서 **"문턱 바로 위(8~12%) 시간들을 8% 아래로 내리는 것"**이 가장 효율적이라는 점에 착안했다.

### 방법: 예측값 구간별 편향 보정 (최근창)

조건부 규칙(착빙·풍향·태풍 등)은 표본이 작아 과적합돼 실패했고, 실제로 통한 건 **예측값 크기별 구간 보정**이었다:

1. 예측값을 크기순으로 여러 구간으로 나눔
2. 각 구간에서 **"실제값 − 예측값"의 평균 편향**을 계산
3. 예측할 때 그 구간의 편향만큼 이동

여기에 두 가지가 결정적이었다:
- **그룹별 세밀도·기간을 다르게 튜닝** (특히 group3는 구간을 촘촘히)
- **"최근 데이터"로 편향을 계산** (전체가 아니라 최근 기간)
- 착빙·태풍 등 이상 시간대는 학습에서 정제

### 왜 이 방법이 통했나 — 2025는 "발전 효율이 떨어진 해"

같은 바람 세기(8~10m/s)에서 실제 발전량을 연도별로 보면 **3개 그룹 모두 하락**하고 있었다:

| 그룹 | 2023 | 2024 | 기존모델의 2025 예측 |
|---|:---:|:---:|:---:|
| group3 | 9,232 | **8,277** ↓ | **10,231** (과대예측) |
| group2 | 11,635 | 9,994 | 12,247 |
| group1 | 11,203 | 9,566 | 11,564 |

- 설비 축소·출력제한·노후 등으로 **"바람 → 발전"의 전환 효율이 해마다 낮아지는** 중.
- 기존 모델은 2022~2024로 학습해서 **2025를 과대예측**한다(특히 group3).
- **"최근 데이터로 편향을 보정"**하면 이 하락의 **가장 최근 수준**을 반영하므로, 2025의 낮아진 실제에 예측이 가까워진다. (전체 기간으로 보정하면 하락 평균만 잡아서 부족.)

즉 팀원 방법의 성공 이유는 **"최근 편향 보정이 2025의 변화(발전 효율 하락)를 자동으로 따라잡았기 때문"**이다. 결과적으로 8~12%에 걸쳐 있던 시간들이 문턱 아래로 내려가 **FICR이 +0.007** 올랐다.

---

## 2. v35 — 팀원 방식을 이어받아 만든 버전 (0.64129 → 0.64199)

### 착안: 가장 확실한 레버(group1 보정)가 절반만 쓰였다

팀원 버전의 그룹별 보정 강도를 뜯어보니, **group1 보정은 50% 강도(수축)로만** 적용돼 있었다.
- group1 보정은 **가장 크고 안정적인 레버**다. group1은 구조적으로 계속 **과소예측**되는 경향이 있어서, 위로 올려주면 오차가 줄고 FICR이 크게 오른다.
- 즉 **아직 절반의 개선 여지가 남아 있었다.**

### 방법: group1 보정을 최대 강도(100%)로 보강

- group1의 최근창 편향을 다시 계산해, 팀원이 쓴 50%에 **나머지 50%를 더해 100%(풀강도)로** 강화.
- group2·group3는 팀원 값을 그대로 유지(이미 검증된 값).
- 편향은 과거(연말) 데이터로만 계산 → 미래 정보 사용 없음(규칙 준수).

이 결정은 별도의 시간분리 검증에서 **"group1 최근창 보정이 성능을 크게 올린다"**는 결과로도 뒷받침됐다(group1은 연중 안정적인 구조적 편향이라 검증이 신뢰 가능).

### 결과: FICR을 더 얻고 총점 상승

| 지표 | 팀원 | v35 | 변화 |
|---|:---:|:---:|:---:|
| 총점 | 0.64129 | **0.64199** | **+0.0007** |
| FICR | 0.4085 | **0.41109** | **+0.0026** |
| 1−NMAE | 0.87408 | 0.87288 | −0.0012 |

**왜 좋아졌나**: 과소예측되던 group1을 더 강하게 올리니, **문턱 위(8~12%)에 있던 group1 시간들이 8% 아래로 넘어가** 정산금을 얻었다(FICR +0.0026). 일부를 과하게 올려 NMAE는 소폭(−0.0012) 손해봤지만, **FICR로 얻은 이득이 더 커서 총점이 순증(+0.0007)**했다. (FICR 우선 트레이드오프.)

---

## 전체 흐름 한눈에

```
기존 A모델 0.63625  ── 2025가 학습기간과 달라 정체
   │
   │  [팀원] 최근 데이터로 구간별 편향 보정
   │         → 2025의 발전효율 하락을 반영 (group3↓ 등)
   ▼
팀원  0.64129  ── 8~12% 시간들이 문턱 아래로 → FICR +0.007
   │
   │  [v35] 같은 방식에서, 절반만 쓰인 group1 보정을 100%로 보강
   ▼
v35   0.64199  ── group1 추가 교정 → FICR +0.0026, 총점 +0.0007
```

**핵심 요약**: *2025는 발전 효율이 떨어진 해였고, 팀원의 "최근 데이터 기반 구간별 편향 보정"이 그 변화를 따라잡아 점수를 올렸다. v35는 그 방식에서 가장 확실한 group1 보정이 절반만 적용된 것을 최대 강도로 채워 한 계단 더 끌어올렸다.*

---
---

# [이어서] v39·v41 개선 정리 (0.64199 → 0.64404) | 팀 공유용

> 목적: 점수를 올린 **방법과 이유**를 정리해, 팀원 각자가 자기 작업에 바로 참고·적용할 수 있게 함.

## 요약 — 버전별 점수 흐름 (전체)

| 버전 | 총점 | 1−NMAE | FICR | 핵심 |
|---|:---:|:---:|:---:|---|
| baseline (v32) | 0.63625 | 0.87117 | 0.40133 | 정체 지점 |
| 팀원 (submit_A) | 0.64129 | 0.87408 | 0.4085 | **최근창 편향보정** (2025 이동 반영) |
| v35 | 0.64199 | 0.87288 | 0.41109 | g1 구조적 강화 |
| v39 | 0.64238 | 0.87179 | 0.41297 | g1 추가 강화 |
| **v41** | **0.64404** | 0.87098 | 0.41710 | **발전량 가중 편향 추정 (g3)** ⭐ |

**한 줄**: 팀원의 **최근창 편향보정**이 2025 변화를 잡아 뚫었고, 거기에 **g1 강화 → 발전량 가중 보정**을 얹어 v41(0.64404)까지 올렸다. 팀원 대비 **총 +0.00275**.

---

## 큰 그림 — 왜 점수가 올랐나

두 축을 동시에 공략했다:

1. **2025 분포 이동**: 학습기간(2022~24)과 달리 2025는 설비 하락·바람 변화로 "바람→발전 전환 효율"이 달라졌다. baseline은 2025를 잘못 예측(특히 g3 과대예측). → **최근 데이터로 편향을 보정**해 따라잡음.
2. **FICR 지표 정조준**: FICR은 **오차 8% 이하일 때만, 발전량 가중으로** 정산금이 생기는 계단형 지표. → 보정을 **발전량 가중**으로 하니 지표에 정확히 맞음.

---

## 핵심 방법 3가지 (각자 모델에 적용 가능)

### ① 최근창 구간별 편향 보정 (팀원 방법 — 가장 중요한 토대)

**아이디어**: 예측값을 크기순 N구간으로 나눠, 각 구간에서 `median(실제 − 예측)`만큼 이동. 단, **편향을 "최근 데이터"로만 계산**.

```
1. 예측값을 크기순 N구간(qcut)으로 나눔
2. 각 구간에서 편향 = median(실제값 − 예측값)  ← 최근 30~45일 데이터만 사용
3. 예측할 때 그 구간 편향만큼 이동 (interp)
```

- **왜 "최근창"이 핵심**: 전체기간으로 보정하면 하락의 *평균*만 잡아 부족. **최근(연말 2024)으로 보정하면 하락의 끝점**을 반영 → 2025의 낮아진 실제에 맞음.
- 그룹별 세밀도/기간 튜닝: g1(6구간·30일), g2(6구간·45일), **g3(20구간·45일, 촘촘히)**.
- 학습데이터 정제: 착빙 시간 제외, 태풍 시간 가중 축소.

### ② g1 구조적 강화 (v35 → v39)

- g1은 **연중 안정적으로 과소예측**되는 구조적 편향이 있음 → 위로 강화하면 오차가 줄고 FICR↑.
- 최근창 보정 강도를 단계적으로 올림(팀원 부분적용 → 풀강도 이상). 리더보드로 정점 탐색.
- **주의**: g1에만 통함. g2/g3는 이미 최적이라 더 밀면 과교정(손해).

### ③ 발전량 가중 편향 추정 (v41 돌파 — 가장 큰 단일 점프) ⭐

**핵심 통찰**: FICR은 **발전량 가중** 지표인데, ①의 편향을 `median`(무가중)으로 구하면 저발전·고발전 시간을 동등 취급 → 지표와 어긋남. **편향을 발전량으로 가중**해서 구하면 고발전 시간(FICR을 좌우)을 정확히 반영.

```python
# 무가중(기존):   편향 = median(실제 − 예측)
# 발전량가중(신규): 편향 = 가중median(실제 − 예측,  weights = 실제발전량)
def wmedian(v, w):
    o = np.argsort(v); v, w = v[o], w[o]; cw = np.cumsum(w)
    return v[np.searchsorted(cw, cw[-1]/2.0)]
```

- **효과 (오프라인 2023→2024 검증)**: g3 **+0.0294**, g2 +0.0039, g1 −0.0208.
- **적용**: **g3에 적용**(가장 큰 효과), **g1은 무가중 유지**(가중이 오히려 해침). g2는 검증됐지만 변화폭이 커서(리스크) 보류.
- **리더보드 실측**: g3만 적용 → **+0.00166 (단일 최대 점프)**. 오프라인 검증이 그대로 전이됨.

---

## 반드시 알아야 할 프레임워크 (판단 기준)

### A. 오프라인 2-fold(2023/2024)는 2025 "이동"에 눈멂
- 2023/2024를 더 잘 맞추는 개선은 2025로 **안 넘어갈 수 있음**(분포 이동 때문).
- 그래서 **"이동 보정"류(최근창 강도 등)는 리더보드가 최종 판정**.

### B. 단, private 테스트셋이 있음 → "구조적 개선"을 우선
- 최종 순위는 **우리가 못 보는 private 데이터**로 평가.
- **오프라인으로 검증되는 "구조적" 개선**(발전량 가중, 물리 기반 등)은 **private에 전이됨 → 안전.**
- **근거 없이 리더보드 숫자만 밀어올리는 것**(public fitting)은 private에서 무너질 수 있음 → 위험.
- **구분법**: 물리적·지표적 근거가 있나? → 있으면 구조적(써도 됨), 없으면 리더보드 노이즈(위험).

---

## 실패한 것들 (시간 낭비 방지 — 다시 하지 말 것)

| 시도 | 결과 | 이유 |
|---|---|---|
| 밴드 경계 재가중 / ficr 직접최적화 | ❌ | 2024 과적합, 2025 전이 실패 |
| 계절(월별) 보정 | ❌ | 2023/2024 상관 −0.14 = 노이즈 |
| 잔차 스태킹 (g2/g3) | ❌ | 이동 때문에 과적합(g3 −0.05) |
| g2/g3 무근거 증폭 (v37) | ❌ | 이미 최적, 과교정 |
| 최근 가중 재학습 | ❌ | 거의 안 움직임 |
| cut-out/이상기상 0처리 | ❌ | 우리 사이트는 cut-out 거의 없음(고풍속=정격, 오히려 과소예측) |

---

---
---

# [최종] v42 = 0.64410 (현재 최고) + 그 이후 검증 기록

## 최종 점수 흐름

| 버전 | 총점 | 1−NMAE | FICR | 핵심 | 결과 |
|---|:---:|:---:|:---:|---|:---:|
| baseline (v32) | 0.63625 | 0.87117 | 0.40133 | 자체 앙상블 | 기준 |
| 팀원 (submit_A) | 0.64129 | 0.87408 | 0.4085 | 최근창 편향보정 | ✅ |
| v35 | 0.64199 | 0.87288 | 0.41109 | g1 강화(+0.5×편향) | ✅ |
| v39 | 0.64238 | 0.87179 | 0.41297 | g1 추가강화(+0.3×편향) | ✅ |
| v41 | 0.64404 | 0.87098 | 0.41710 | **g3 발전량가중** | ✅ 최대점프 |
| **v42** | **0.64410** | **0.86947** | **0.41873** | **g2 발전량가중 50%** | ✅ **최고** |
| v43 | 0.64322 | 0.87298 | 0.41345 | g1 2차보정(하향) | ❌ |
| v44 | 0.64335 | 0.86932 | 0.41738 | 일주기(시각) 보정 | ❌ |
| v45 | 0.64091 | 0.86746 | 0.41436 | SCADA 2단계 베이스 | ❌ |

**최종 = v42 (0.64410)**. 팀원 대비 **+0.00281**, baseline 대비 **+0.00785**.

## v42 만드는 법 (4단계)

```
팀원 submit_A (최근창 편향보정 완료본)
   │
   │ ① g1 += 0.5 × 최근창편향(2024 마지막 30일, 6구간, 무가중)
   ▼  v35
   │ ② g1 += 0.3 × 같은 편향                    → g1 최종 평균 9,166
   ▼  v39
   │ ③ g3 += 1.0 × (발전량가중median − 무가중median)   [45일, 20구간]
   ▼  v41                                        → FICR +0.004 (최대 점프)
   │ ④ g2 += 0.5 × (발전량가중median − 무가중median)   [45일, 6구간]
   ▼  v42 = 0.64410
```

- ③④의 보정량은 **v32 raw 예측값**을 기준으로 보간해서 계산 (팀원 보정 위에 얹는 구조).
- 편향은 전부 **2024년 OOF 예측 vs 실측**으로 계산 → 미래정보 없음(규칙 준수).

## v42 이후: 무엇을 더 시도했고 왜 실패했나

### ❌ v43 — g1 2차 잔차보정
독립기간(1차 학습창과 안 겹침)으로 검증해 Δ+0.0022 나왔으나 리더보드 −0.0008.
**교훈: 오프라인 검증을 아무리 엄격하게 해도 2025 분포이동은 못 봄.**

### ❌ v44 — 일주기(시각) 보정
새 조건축 탐색에서 발견. 레벨 제거 후 변동 상관 2023vs2024 **+0.77, 부호일치 7/8**,
g1·g2가 독립적으로 **동일 위상**(오전 과소예측/오후 과대예측 = NWP 경계층 물리).
우리가 만든 가장 엄격한 테스트를 통과했는데도 리더보드 −0.0008.

### ❌ v45 — SCADA 실측풍속 2단계 모델
`train/scada_{vestas,unison}_train.csv`에 **터빈 17대 실측 풍속/풍향**이 있는데 안 쓰고 있었음.
`예보 → 실측 사이트풍속 → 발전량` 2단계 구성 → 풍속 MAE **1.82 → 1.005 m/s**, g1 raw **+0.0075**.
**그러나 최근창 보정이 이미 같은 편향을 잡고 있어 중복** → 보정 후 이득 소멸, 리더보드 −0.0032.

## g1 최적점 확정 (실측 3점)

| g1 평균 | 리더보드 |
|:---:|:---:|
| 8,861 (v43) | 0.64322 |
| **9,166 (v42)** | **0.64410** ⭐ |
| 9,942 (v45) | 0.64091 |

**양방향 모두 손해 → g1은 9,166이 정점. 더 건드릴 필요 없음.**
("g1은 올릴수록 좋다"는 9,036→9,166 좁은 구간에서만 참이었고, 외삽하면 틀림.)

## 핵심 진단 — 왜 더 안 오르나

1. **우리 오차(11.4~12.7%용량)가 "예보 풍속만으로 가능한 하한"(12.1~13.2%)보다 이미 낮음**
   → 주어진 예보에서 짜낼 수 있는 신호는 다 짜냄.
2. **실측 사이트풍속을 "안다면"** 오차 11.5% → **5.4~6.6%**, 8%초과 55% → **19~37%**
   → 격차의 정체는 **풍속 정보의 정확도**. 하지만 2025 SCADA가 없어 직접 사용 불가.
3. **학습 가중치도 이미 최적** (현재 `0.5+y/cap`; 대안 전부 ±0.001 이내).

→ **후처리·재학습 레버는 전부 소진.** 남은 여지는 **원시 격자 예보 데이터의 피처 엔지니어링 재설계**
(현재는 격자 평균 + 일부 격자별 풍속만 사용 / LDAPS 130MB·GFS 80MB 원본에 미사용 정보 다수).

## 실패 목록 (다시 하지 말 것)

| 시도 | 이유 |
|---|---|
| 밴드 경계 재가중 / FICR 직접최적화 | 2024 과적합 |
| 계절(월별) 보정 | 2023vs2024 상관 −0.14 = 노이즈 |
| 잔차 스태킹 (g2/g3) | 분포이동으로 과적합 (g3 −0.05) |
| g2/g3 무근거 증폭 (v37) | 이미 최적, 과교정 |
| 최근가중 재학습 | 거의 안 움직임 |
| cut-out/이상기상 0처리 | 이 사이트는 컷아웃 거의 없음(고풍속=정격) |
| 곱셈(비율) 보정 | 각자 최적강도로 비교해도 덧셈과 동률(±0.0004) |
| 6%문턱 2차보정 | g2/g3 신호없음, g1은 리더보드 실패(v43) |
| 일주기 보정 | 엄격검증 통과했으나 리더보드 실패(v44) |
| SCADA 2단계 | raw는 개선되나 기존 보정과 중복(v45) |

## 방법론 교훈 (팀원 참고용)

1. **강도는 항상 0~1.2 스윕할 것.** g1/g2/g3 전부 "100%가 최적"이 아니라 **중간(50~60%)에 정점**이 있었음.
2. **2차보정을 1차와 같은 기간으로 학습하면 순환참조** → 가짜 신호/가짜 기각 둘 다 발생. 반드시 독립기간으로 재확인.
3. **새 조건축 후보는 "레벨 제거 후 변동"의 연도간 상관으로 판정.** 레벨만 공통이면 축신호가 아님.
4. **오프라인 검증은 구조적 개선에만 신뢰.** 2025 분포이동 관련은 리더보드가 최종 판정자.
5. **단, 오프라인이 명확히 반대하면 베팅하지 말 것** (v45가 그 실수 — 오프라인 경고를 무시하고 실패).

---

*작성 근거: 리더보드 실측 점수(v32~v45), step46~84 실험 로그. 코드: `BARAM2026_v42_전체코드.py`*
