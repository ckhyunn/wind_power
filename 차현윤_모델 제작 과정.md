# 제3회 풍력발전량 예측 AI 경진대회 (BARAM 2026) — 프로젝트 전체 정리

> 기상 예보로 3개 발전소 그룹의 **2025년 시간별 발전량**을 예측하는 프로젝트.
> 이 문서는 데이터·전처리·피처·모델·기법·버전 히스토리를 배경지식 없이도 이해할 수 있게 정리한 것입니다.

---

## 1. 프로젝트 개요

- **목표**: 기상 예보(LDAPS·GFS)를 입력으로, KPX 3개 그룹(group1·2·3)의 **매 시간 발전량(kWh)**을 예측
- **입력(X)**: 미래 시점의 기상 예보 (풍속·온도·기압·구름 등)
- **정답(y)**: KPX 실제 발전량
- **평가 산식**: `Score = 0.5 × (1 − NMAE) + 0.5 × FICR`
  - **NMAE**: 평균 오차(작을수록 좋음). "얼마나 정확한가"
  - **FICR**: 오차가 **용량의 6~8% 밴드 안**에 드는 시간의 비율(발전량 가중). "얼마나 자주 충분히 정확한가"
  - 발전량이 설비용량의 10% 미만인 시간은 채점 제외

### 한눈에 보는 구조
```
기상예보(LDAPS/GFS) ──▶ 전처리 ──▶ 피처 엔지니어링 ──▶ 모델 ──▶ 발전량 예측
      원본 데이터                     98~192개 변수(X)      앙상블      y (kWh)
```

---

## 2. 데이터 소개 & 전처리

### 2-1. 사용 데이터

| 데이터 | 내용 | 역할 |
|--------|------|------|
| **ldaps_train/test** | 한반도 지상 정밀 기상 예보 (격자 16개) | 입력 X의 원천 |
| **gfs_train/test** | 전지구 기상 예보 (상공 포함, 격자 9개) | 입력 X의 원천 |
| **train_labels** | KPX 3개 그룹 실제 발전량 | 정답 y |
| **scada_vestas** | vestas 터빈 12기 실측 발전량·풍속 (10분) | g1·g2 참고/검증 |
| **scada_unison** | unison 터빈 5기 실측 발전량·풍속 (10분) | g3 참고/검증 |

- **그룹↔단지 대응**: g1·g2 ≈ **vestas 단지**(상관 0.988·0.989), g3 ≈ **unison 단지**(상관 0.997)
- SCADA는 **미래(test)엔 없으므로** 예측 입력으로 못 쓰고, **라벨 검증·데이터 이해**에만 사용

### 2-2. 전처리(데이터 정제)

원본 SCADA에 물리적으로 불가능한 값이 있어 정제:
- **부호쌍 오류(음양 반복)**: 연속된 값이 +X, −X처럼 뒤집힌 오류 → 수정
- **비정상 음수**: 발전량 음수(불가능) → 0 처리
- **극단 이상치**: 수백만 kW 등 말도 안 되는 값 → 제거
- **결측 처리**: 물리적으로 불가능한 값 결측화

→ 정제 결과 `scada_*_clean.csv` 생성. **원본은 절대 수정하지 않고 복제본으로 작업.**

---

## 3. 피처 엔지니어링 (입력 변수 만들기)

### 3-1. 왜 필요한가
피처 엔지니어링 = **원본 데이터를 모델이 이해하기 쉬운 "재료"로 손질**하는 일.
모델은 숨은 관계를 스스로 잘 못 찾으므로, **물리 지식을 미리 계산해서** 떠먹여 주면 훨씬 잘 맞힘.
> 실제로 우리 성능 향상의 상당 부분이 모델 교체가 아니라 **피처 손질**에서 나왔습니다.

### 3-2. 핵심 손질들

| 손질 | 내용 | 이유 |
|------|------|------|
| **u,v → 풍속** | 바람 동서(u)·남북(v) 성분 → `ws=√(u²+v²)` | 발전량은 바람 세기(풍속)로 결정 |
| **풍속 → ws³** | 풍속의 세제곱 | 발전량 ∝ 풍속³ (바람 2배 → 발전 8배) |
| **공기밀도** | `ρ = 기압/(287×온도)` | 발전량 = ½·ρ·A·v³·Cp |
| **발전량 프록시** | `ρ × ws³` | 물리 발전량 근사 |
| **격자 집계** | 주변 격자들을 평균/최대/최소/표준편차로 요약 | 발전소 전체 대표 바람 |
| **격자별 피처** | 격자를 평균 안 내고 **각각** 피처로 | 어느 격자가 어느 그룹에 중요한지 학습 |
| **풍향(sin/cos)** | 방향을 sin·cos로 | 후류·지형 채널링 반영 |
| **lag/lead** | 앞뒤 1~3시간 풍속 | 바람 급변(ramp) 포착 |

> **중요**: u,v를 **먼저 평균 내면 안 됨**. 방향이 반대인 바람끼리 상쇄되어 강풍이 0으로 사라짐(벡터 캔슬).
> **격자마다 풍속을 먼저 계산한 뒤** 집계해야 정확.

### 3-3. 최종 사용 피처 (그룹별)
- **시간**: month, hour, hour_sin/cos, doy_sin/cos (계절·시간대 패턴)
- **LDAPS 바람**: 여러 높이(10·50m) 풍속·돌풍·ws³·발전량프록시·풍향·근접격자
- **LDAPS 대기**: 공기밀도·온도·습도·기압·경계층높이·구름·강수
- **GFS 바람**: 10·80·**100m(허브높이)**·**850hPa(상공 강풍)** 풍속·집계·풍향
- **GFS 기타**: 돌풍·환기율·전운량·강수율
- **시간 변화(ramp)**: 변화율(diff)·이동평균·lag/lead
- **격자별 피처**: 16+9 격자의 풍속·ws³·풍향 (공간 정보)
- **예보/그룹**: 리드타임(lead_hours), group_id(통합학습용)

---

## 4. 모델 & 사용 기법 (쉽게 설명)

### 4-1. 기본 개념

**① 결정 트리 (Decision Tree)** = 스무고개.
"풍속 5 이상? →예→ 온도 10도 이상? →아니오→ 발전량 3000" 식으로 질문을 이어가 답에 도달하는 나무.

**② 그래디언트 부스팅 (Gradient Boosting)** = 세 모델의 공통 뿌리.
트리 한 개는 약해요. 그래서 이렇게 합니다:
```
1번 트리: 대충 예측 →  남은 오차(잔차)를
2번 트리: 그 오차를 맞힘 →  또 남은 오차를
3번 트리: 그걸 또 맞힘 →  ... 수백 번 반복
```
**"남은 오차를 계속 따라가며 줄인다"**고 해서 gradient(경사) boosting.
우리가 쓰는 3모델은 **전부 이 방식**이고, 차이는 **"트리를 어떻게 만드느냐"**에 있어요.

### 4-2. 우리가 쓰는 모델 3종 — 각각 쉽게

**🟩 XGBoost (엑스지부스트)** — "원조 격, 튼튼한 기본기"
- 그래디언트 부스팅을 **대중화한 대표 모델**. 캐글 대회에서 수없이 검증됨.
- 트리를 **층별로(level-wise) 고르게** 키우고, **규제(regularization)를 강하게** 넣어 과적합을 억제.
- 특징: 안정적이고 믿을 수 있음. → 우리 앙상블에서 **든든한 기본기** 역할.

**🟦 LightGBM (라이트지비엠)** — "빠르고 정확한 주력"
- XGBoost를 **훨씬 빠르게** 개량한 모델. 우리 **주력(비중 최대)**.
- 두 가지 핵심 트릭:
  - **히스토그램**: 연속값(풍속 등)을 구간으로 묶어 계산량을 확 줄임 → 대용량에 빠름
  - **잎 우선 성장(leaf-wise)**: 트리를 층층이 고르게가 아니라, **가장 이득 큰 가지부터 깊게** 키움 → 같은 시간에 더 정확
- 특징: 대용량·다변수에 빠르고 강함. → 그래서 **비중을 제일 크게(예: 0.50)**.

**🟧 CatBoost (캣부스트)** — "과적합에 강한 안정형"
- **과적합 방지에 특화**된 모델.
- 핵심 트릭 **순서형 부스팅(ordered boosting)**: 각 단계에서 **그 시점까지의 과거 데이터만** 사용해 오차를 계산 → 미래를 몰래 보는 편향을 줄임.
- 결측치·범주형 데이터 처리가 깔끔하고 안정적. → 앙상블에서 **다른 관점의 안정감** 제공.

**정리**
| 모델 | 트리 만드는 방식 | 강점 | 우리 비중 |
|------|------------------|------|:---:|
| XGBoost | 층별 + 강한 규제 | 안정·검증됨 | 중 |
| LightGBM | 잎 우선 + 히스토그램 | 빠르고 정확 | **최대** |
| CatBoost | 순서형 부스팅 | 과적합에 강함 | 중 |

### 4-3. 앙상블 (Ensemble) — 세 모델을 왜 섞나
- 세 모델을 **가중 평균**으로 합침 (예: LightGBM 0.50 + CatBoost 0.10 + XGBoost 0.40).
- **만드는 방식이 다르니 틀리는 지점도 서로 다름** → 평균 내면 **엉뚱한 큰 오차가 상쇄** → 개별 모델보다 정확·안정.
- 실제로 우리 프로젝트에서 XGBoost를 추가(다양성)한 것만으로 점수가 올랐습니다(v18).

### 4-4. 사용한 기법 총정리 (쉬운 설명)

| 기법 | 무엇인가 | 왜 쓰나 |
|------|----------|---------|
| **2-fold 검증** | 과거로 만든 "모의고사". 2023 학습→2024 시험, 2024 학습→2025 시험. | 제출 전 실력 확인. 제출 기회가 제한돼 좋은 것만 냄 |
| **가지치기(피처 선택)** | 모델이 잘 안 쓰는 **중요도 하위 30% 변수 제거** (중요도 = 그 변수가 예측에 기여한 정도) | 쓸모없는 재료는 노이즈 → 잘라 정리 |
| **순환 인코딩(sin/cos)** | 시각·풍향을 sin·cos 두 값으로 변환 | 23시→0시, 359°→0°가 **끊기지 않고 이어지게**(원형이라) |
| **MAE 학습(objective=l1)** | 오차를 절댓값으로 학습(제곱 아님) | 채점(NMAE)과 같은 방식으로 → 채점에 유리 |
| **발전량 가중치** | 발전량 큰 시간을 더 중요하게 학습 | 점수(FICR)가 고발전에서 크게 갈림 |
| **10% 컷** | 용량 10% 미만 시간은 학습 제외 | 대회가 그 시간을 채점 안 함 |
| **통합 학습(pooled)** | 데이터 적은 g3를 g1·2와 합쳐 학습 | 다른 그룹 패턴을 빌려 보강 |
| **라벨 디노이징** | SCADA와 크게 어긋난 라벨을 학습에서 제외 | 틀린 정답으로 배우면 혼란 |
| **시드 평균** | 같은 모델을 랜덤값만 바꿔 5번 학습·평균 | 우연한 이상 학습을 평균으로 안정화 |
| **스무딩** | 시간별로 튀는 예측을 3시간 중앙값으로 완화 | 실제 발전량은 급격히 안 튐 |
| **Optuna 튜닝** | 모델 설정값(나무 수·깊이·규제)을 자동 수백 번 탐색 | 사람이 못 하는 최적 조합 자동 탐색 |
| **편향 보정(calibration)** | 예측이 체계적으로 낮게/높게 나오면, 발전량 구간별로 그만큼 더하거나 빼서 교정 | 큰 오차(FICR 0점)를 밴드 안으로 구제 (실제로 g1이 과소예측이라 위로 보정) |
| **블렌드 가중치 최적화** | 세 모델을 몇 대 몇으로 섞을지 자동 탐색 (2023에서 고르고 2024로 확인) | 튜닝 후 모델 강도가 바뀌면 최적 비율도 바뀜 → 재조정 |
| **격자별 피처** | 격자를 평균 안 내고 각각 입력 (16+9 격자의 풍속·ws³·풍향) | 어느 격자가 어느 그룹에 중요한지 학습 → 공간 정보 살림 |
| **클리핑(clip)** | 예측값을 0 ~ 설비용량 범위로 자름 | 음수 발전량·용량 초과는 물리적으로 불가능하니 |

### 4-4-1. 기법 하나씩 자세히

#### ▸ 검증 & 데이터 관리
- **2-fold 검증** — 진짜 시험(2025)을 못 보니 **과거로 모의고사**를 만든다. 2023년까지 공부→2024년 맞혀보기처럼 "과거 학습→나중 맞히기". **시간 순서를 지키는 게 핵심**(랜덤으로 섞으면 미래를 몰래 보는 반칙=데이터 누수). 두 번(2023·2024) 시험해 우연이 아닌지 확인하고, 2025에 가까운 2024를 더 신뢰. **양쪽 시험 다 오른 것만 제출.**
- **가지치기(피처 선택)** — 변수(최대 192개) 중 모델이 실제로 잘 안 쓰는 **하위 30%를 잘라냄**. "중요도"는 각 변수가 예측을 나누는 데 얼마나 기여했는지로 잰다. 쓸모없는 변수는 오히려 헷갈리게 하니 정리.
- **10% 컷** — 대회는 발전량이 설비용량 10% 미만인(거의 안 도는) 시간을 **채점에서 뺀다**. 그래서 학습도 그 시간을 빼고 채점되는 "발전 활발한 시간"에 집중.
- **통합 학습(pooled)** — g3는 데이터가 2년치로 적다. g1·g2 데이터와 **합쳐 한 모델로** 배우되, 용량으로 정규화하고 "어느 그룹인지"를 표시(group_id). 데이터 많은 형들의 패턴을 빌려 g3를 보강.
- **라벨 디노이징** — 정답(라벨)에도 오류가 있을 수 있다. g3는 unison 실측(SCADA)과 1000kWh 넘게 어긋나는 시간을 **라벨 오류로 보고 학습에서 뺌**. 틀린 답으로 배우면 모델이 헷갈리니까.

#### ▸ 학습 방식
- **MAE 학습(l1)** — 보통은 오차를 제곱해서 배우는데(큰 오차에 과민), 우리는 **절댓값으로** 배운다. 대회 채점(NMAE)이 절댓값 기반이라 **채점 방식과 똑같이** 맞춤.
- **발전량 가중치** — 학습 때 모든 시간을 똑같이 보지 않고 **발전량 큰 시간에 더 무게**(0.5 + 발전량/용량). 점수(FICR)가 고발전 시간에서 크게 갈리니 거길 더 정확히.
- **순환 인코딩(sin/cos)** — 시각(0~23)·풍향(0~360°)은 **원형**이라 "23시 다음이 0시"인데 숫자론 뚝 끊긴다. sin·cos 두 값으로 바꾸면 부드럽게 이어져 모델이 주기를 이해.
- **Optuna 튜닝** — 모델엔 사람이 정할 설정값(나무 수·깊이·규제 강도 등)이 많다. Optuna가 이 조합을 **자동으로 수백 번 시험**해 2-fold 점수가 제일 좋은 걸 찾아준다. (우리는 "더 얕고 규제 강한" 조합을 찾아 과적합을 눌렀다.)

#### ▸ 앙상블 & 안정화
- **시드 평균** — 같은 모델도 랜덤 초기값(seed)에 따라 조금씩 다르게 학습된다. **5개 seed로 학습해 평균** 내면 우연한 튐이 상쇄돼 안정적.
- **블렌드 가중치 최적화** — 세 모델을 **몇 대 몇으로 섞을지**(예 0.5/0.1/0.4)를 2023에서 여러 조합 시험→2024로 확인해 결정. 튜닝으로 모델 강도가 바뀌면 최적 비율도 바뀌니 재조정.

#### ▸ 후처리 (예측을 낸 뒤 다듬기)
- **스무딩** — 같은 예보 블록 안에서 시간마다 예측이 들쭉날쭉하면 **3시간 이동 중앙값**으로 부드럽게. 실제 발전량은 갑자기 확 안 튀니까.
- **편향 보정(calibration)** — 예측이 특정 그룹에서 **체계적으로 낮거나 높게** 나오면(예: g1은 늘 조금 낮게), 발전량 구간별로 그 편향만큼 더하거나 빼서 교정. 8% 밖(0점)이던 시간을 밴드 안으로 구제 → FICR↑. **(우리 최대 효과 레버 중 하나 — g1 과소예측 보정)**
- **클리핑(clip)** — 마지막에 예측을 **0 ~ 설비용량**으로 자른다. 음수 발전량·용량 초과는 물리적으로 불가능하니.

#### ▸ 피처
- **격자별 피처** — 발전소 주변 격자(LDAPS 16·GFS 9)를 **평균 내 버리지 않고 각각** 입력으로. group_id와 결합하면 모델이 "어느 격자 바람이 어느 그룹에 중요한지" 스스로 학습 → 공간 정보를 살려 정확도↑. **(풍속·ws³·풍향까지 격자별로 확장한 게 후반부 최대 개선원)**

### 4-5. "과적합" 방어
피처가 많아도(최대 192개) 과적합을 막는 4중 장치:
1. **가지치기**(하위 30% 제거) 2. **2-fold 검증**(양쪽 fold 다 개선해야 채택)
3. **트리 규제**(규제 강한 파라미터를 Optuna가 자동 선택) 4. **앙상블·시드 평균**(분산 축소)
> **증거**: 리더보드(처음 보는 2025 데이터)가 계속 상승 → 외운 게 아니라 일반화되고 있다는 뜻.

---

## 5. 버전별 발전 과정 (핵심 흐름)

> 원칙: **새 아이디어 → 2-fold 검증 → 양쪽 fold 다 이길 때만 제출 → 리더보드 확인.**
> 통과 못 한 실험은 파일을 안 만듦(과적합 방지). 개선된 버전이 다음 실험의 새 베이스가 됨(누적).

| 단계 | 버전 | 핵심 변화 | 리더보드 |
|------|------|-----------|:---:|
| 피처 구축 | v4 | 풍속·ws³ 기본 피처 완성 | 0.6126 |
| | v7~v8 | 예보 정리·피처 정리 | 0.615~0.619 |
| 정교화 | v11 | 통합학습·디노이징 | 0.6263 |
| **발전량 가중** | **v14** | 발전량 가중치 도입 (한동안 최고) | **0.6284** |
| FICR 시도(실패) | v15~v17 | FICR 직접 최적화 → 전부 실패 | 0.627~0.628 |
| **앙상블 다양성** | **v18** | XGBoost 추가 | **0.6290** |
| **Optuna 튜닝** | **v20** | LightGBM 하이퍼파라미터 튜닝 | **0.6297** |
| **편향 보정** | **v22b** | g1 과소예측 보정 (FICR 대폭↑) | **0.6322** |
| 앙상블 재튜닝 | v23 | Cat·XGB 튜닝 | 0.6323 |
| **격자별 피처** | **v28** | 격자별 풍속 (공간 정보) | **0.6345** |
| | **v29** | 격자별 ws³·상공·80m 확장 | **0.6349** |
| 격자 재튜닝 | v30 | 157피처 재튜닝 (과적합→소폭 퇴보) | 0.6347 |
| **격자별 풍향** | **v32** | 격자별 풍향(후류·채널링), FICR 첫 0.40 돌파 | **0.63625** |

**실패·기각한 실험(과학적 기록)**: FICR 직접 최적화(v15~17), 4번째 모델·MLP 다양성(v19·v31), 블렌드/하이퍼파라미터 재탐색(v30), 밴드 보정(v24), 물리 피처(v25), g3 전용 모델(v26), g1·g2 디노이징(v33) → 모두 2-fold 게이트에서 개선 없음/과적합으로 기각.

### 5-1. 핵심 교훈 (3막 스토리)
1. **피처로 올리기** (v4~v13): 원본을 잘 손질해 0.60 → 0.626
2. **막다른 길** (v15~v17): FICR을 억지로 최적화 → 실패. **교훈: FICR은 직접 못 짜내고, 예측을 근본적으로 정확히 해야 오른다.**
3. **정공법 돌파** (v18~v32): "예측 분산·체계 편향을 줄이면 → 큰 오차(FICR 0점) 감소 → FICR·총점 상승" 원리로,
   **다양성 → 튜닝 → 편향 보정 → 격자 공간 피처**를 차례로 쌓아 0.6284 → 0.635까지.

---

## 6. 성과 & 향후 방향

- **성과**: 0.6126(v4) → **0.63625(v32)**, 특히 **FICR 0.384 → 0.401**로 약점을 크게 개선(첫 0.40 돌파)
- **통하는 레버**: 격자 공간 피처, 편향 보정, 앙상블 다양성, Optuna 튜닝 (모두 2-fold+리더보드로 검증)
- **안 통하는 레버**: fold B만 직접 최적화하는 것(재튜닝·블렌드 재탐색·약한 모델 추가) → 과적합
- **향후**:
  - 격자 광맥 추가 채굴(격자별 돌풍·다른 변수)
  - 팀 협업 블렌딩(각자 **독립적인 모델**을 만들어 합치면 다양성으로 추가 상승)
  - (큰 도약 시) 외부 공개 기상예보 데이터 블렌딩 — 규정상 공개·재현가능·leakage 준수 필요

---

*예측은 모두 로컬 라이브러리(LightGBM·CatBoost·XGBoost·Optuna)로 수행하며, 원격 API 추론·외부 비공개 데이터·데이터 누수(leakage)는 사용하지 않습니다.*
